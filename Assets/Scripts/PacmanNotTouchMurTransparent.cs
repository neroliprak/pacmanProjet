using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PacmanNotTouchMurTransparent : MonoBehaviour
{

    private const string MUR_TRANSPARENT_TAG = "mur_transparent"
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
